{
  "name": "life-rpg",
  "version": "1.0.0",
  "description": "Life RPG - Gamified Life Tracker (monorepo root)",
  "scripts": {
    "start": "node backend/src/server.js",
    "build": "npm install --prefix backend && npm run build --prefix frontend",
    "install:backend": "npm install --prefix backend",
    "install:frontend": "npm install --prefix frontend --legacy-peer-deps"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
